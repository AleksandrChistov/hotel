block variables

doctype html
html(lang="ru")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    link(rel="shortcut icon" href=urlFavicon type="image/x-icon")
    title= pageTitle
  body(class="body" + bg)
    block content